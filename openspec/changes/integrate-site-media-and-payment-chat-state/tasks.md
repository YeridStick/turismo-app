# Tasks

- [ ] A.1 Confirm backend multipart upload endpoint, field names, response media DTO, limits and site creation order.
- [ ] A.2 Implement Expo image picker, validation and per-file local states.
- [ ] A.3 Implement backend-only multipart upload and partial retry without recreating a site.
- [ ] B.1 Confirm persisted media DELETE endpoint and mediaId contract.
- [ ] B.2 Implement coordinated persisted deletion and cache/UI update after success only.
- [x] C.1 Use existing checkout and payment status contracts without direct Wompi calls.
- [ ] C.2 Add bounded, cancelable payment reconciliation after checkout.
- [ ] C.3 Refresh reservation list, detail and open chat messages from backend authority.
- [ ] D.1 Add automated coverage for multimedia states/deletion and payment concurrency.
- [ ] D.2 Run all repository validation commands and record device limitations.

